# CONFIG 生产部署记录

## 2026-09-23 调用入口去重与中性卡片

移除右侧重复的“目录状态”下拉框，只保留卡片“允许调用”开关；表单内部保留隐藏状态字段，避免编辑模型 ID 或主机名时意外重新启用已停用模型。取消停用卡片的暗红底色及红色文字覆盖，使用正常灰色卡片并保留“已停用”“分组停用”状态。

仅更新 `lib/admin-catalog-view.mjs` 和 `lib/admin-editor-view.mjs`，备份为 `/root/flow-config-backups/neutral-cards-20260922T182134Z/`，回执为 `output/config-neutral-cards-deployed.json`。部署前后数据目录摘要一致，保留当时正式 r17、预览 r18；没有改变任何模型的调用或显隐状态。两项既有浏览器流程覆盖单模型/整组停用、编辑、发布、恢复与桌面/手机布局，检查通过；线上服务健康正常。

## 2026-09-23 山海恢复与卡片调用开关

山海四条在“备用分组2”恢复显示和允许调用，正式为 r17、预览为 r16，两站后台同时恢复四模型和山海渠道。CONFIG 调用状态通过现有 `catalog-control` 操作，显隐单独定向修改；没有重部署服务来执行开关。调用状态、显隐和两站后台的备份/回执见 `docs/config-channel-control.md`。

管理页另新增每张模型卡片的“允许调用”开关，修改后通过既有“发布配置”应用到当前通道；停用不再隐藏卡片，组内全部停用仍显示组卡片。部署只更新三个管理 UI 模块及价表中山海八条记录的 `active` 标记，金额不变。代码备份 `/root/flow-config-backups/card-call-switch-20260922T180626Z/`，回执 `output/config-card-call-switch-deployed.json`。本轮代码部署前后正式 r17、预览 r16、密码及数据文件摘要一致，源码画布已自动拉取 r16。

## 2026-09-23 上游成本展示

管理卡片在双站售价下方增加独立成本行，数据由登录保护的 `/admin/model-costs` 下发。当前视频目录 20 条中，11 条有上游参考标价、4 条有明确日期的历史报价、5 条待核对；待核对为 `seedance_v2.5`、Yueqi 未列出的精确 Pro、GlobalAiOpc 和山海两个旧 933 型号。参考报价不是已核实的账户最终结算价，币种、兑换、折扣和查询依据见 `docs/config-model-costs.md`。

仅部署成本接口、投影模块、三个管理 UI 模块及成本种子。服务器备份和回执为 `/root/flow-config-backups/model-costs-20260922T174557Z/`，本地回执为 `output/config-model-costs-deployed.json`。部署保留并行更新后的正式 r12、预览 r13；公开 JSON、数据目录、密码与服务设置摘要均未变化，没有修改中转站售价或启停状态。服务器隔离登录实例确认成本接口正常返回，文件摘要及语法检查通过；37 项相关测试、补充积分单位测试、桌面/移动布局和 Lint 通过。

## 2026-09-23 Yueqi Pro 时长校正

按用户指出的使用范围，将精确模型 `seedance-2.5-pro` 的时长由 1-60 秒收紧为 4-30 秒，保留默认 10 秒、分辨率、参考素材限制与两站 9.80 元/次价格。正式发布 r12、预览发布 r13，仅改目标条目的时长约束和对应说明；原 r11/r10 留档。在线源码画布已自动读取预览 r13。

原 1-60 秒来自 Yueqi 通用公开能力元数据，未经生成边界验证；本次没有调用收费生成接口，也没有更改 `seedance-2.5-pro-720`。部署脚本中的目标模型说明同步修正，回执保留于 `output/pro-duration-config-receipt.json`。

## 2026-09-23 标准账号密码登录

管理登录页增加账号输入，固定管理员账号 `admin`，沿用原密码哈希。用户名和密码分别使用 `autocomplete="username"`、`autocomplete="current-password"`，标准 POST 表单和关联 label 便于浏览器识别保存与自动填充；实际保存提示仍由浏览器设置决定。后端拒绝错误或缺失的用户名，账号和密码失败共用原 IP 限流，错误提示统一为“账号或密码不正确”。预览通道的登录返回路径保留。

仅定向更新线上 `lib/auth.mjs`、`lib/pages.mjs` 与 `server.mjs`，备份 `/root/flow-config-backups/admin-login-20260922T171706Z/`；本地回执 `output/config-admin-login-deployed.json`。公网登录 HTML 已验证包含新字段，服务健康正常。管理密码、余额凭据、所有现行配置及版本文件、环境与服务配置摘要均未变，正式 r11、预览 r10 保持不变。相关单测 28 项、Chrome 桌面/手机隔离登录流程与 Lint 通过，未声称实测浏览器保存密码弹窗。

## 2026-09-23 双站价格核对与展示

管理页操作模式和表单列表已显示老站、新站两行价格，读取登录保护的 `/admin/model-prices`，来源为两站数据库只读核对生成的 `seed/admin-model-prices.json`。两站各 21 条模型均有有效计费规则；价格保留按次、按秒、分辨率和百万 Token 档位，海外 2.0 标注当前全站 9 折。具体规则与未接入条目见 `docs/config-model-prices.md`。

只更新 `server.mjs` 的价格投影及接口、三个管理编辑模块与价格种子文件；保留远端其他功能和本地并行开发。代码备份位于 `/root/flow-config-backups/two-site-prices-20260922T165803Z/`，回执为 `output/config-two-site-prices-deployed.json`。部署后隔离认证实例读取到每站 21 条价格，线上健康检查通过；数据目录所有文件摘要不变，公开正式 r11、预览 r10 内容 SHA256 与部署前完全一致。没有修改任何中转站计费、渠道或密码。

定向接口/模型测试及 ESLint 通过，真实价表的桌面和移动端浏览器检查通过。截图保留于 `output/playwright/config-two-site-prices-*.png`。此次是管理服务更新，无需重发桌面安装包。

## 2026-09-23 上游余额表

管理页新增上游余额表，位置在模型编辑区下方。当前 8 条账户记录中，主播视频已接入实时查询，其余明确显示待接入。页面可见时每 5 分钟刷新，支持手动刷新和可编辑预警线，查询失败保留上次金额并标注。默认主播预警线 CNY 50；部署后的真实查询返回 CNY 44.35。

部署仅更新余额服务、页面和客户端模块，以及余额清单种子；`server.mjs` 以线上版本定向插入余额路由，未夹带工作区另一项价格变更。主播现有 Key 由老站直接读入内存后写入 CONFIG 私有文件 `data/admin-balance-credentials.json`，权限 600、属主 flowconfig；没有保存到本地或发送到浏览器。公开 CONFIG、原数据文件、管理密码和服务配置摘要均未变，正式 r11、预览 r10 保持不变。

备份及回执为 `/root/flow-config-backups/balances-20260922T164739Z/`；本地回执为 `output/config-balances-deployed.json`。定向测试 24 项、桌面/移动端浏览器验证通过；线上健康 200，未登录余额接口 401，服务端余额模块不可作为浏览器资源访问。查询口径和接入边界见 `docs/config-upstream-balances.md`。

## 2026-09-23 管理卡片价格与上游来源

已部署模型卡片的老站价格、上游名称和小字体 URL。正式及源码预览管理页共用更新后的管理模块；来源资料只由登录后的 `/admin/model-sources` 返回，不加入公开 CONFIG。预览 r10 的 20 个视频目录条目均能匹配来源资料；价格仍读取已有老站 `pricing`，缺失报价显示“未配置”。

仅更新 `server.mjs`、`lib/admin-catalog-model.mjs`、`lib/admin-catalog-view.mjs`、`lib/admin-editor.mjs` 和新增的 `seed/admin-model-sources.json`。线上差异逐项核对后暂存、语法检查并重启 `flow-config`。部署文件 SHA256 与本地一致，公网健康检查正常；来源接口和管理脚本仍要求登录，直接访问种子文件返回 404。

正式配置保持 `20260922T154232-r11.json`，预览保持 `20260922T142508-r10.json`；两条公开 CONFIG 内容摘要、数据目录所有文件、管理密码、服务单元、环境配置、schema 与原模型种子摘要均未变化。服务重启使内存中的管理会话失效，需要重新登录。没有修改中转站配置、计费或发布新的模型版本。

服务器定向代码备份和核对回执：`/root/flow-config-backups/admin-display-20260922T160711Z/`。本地回执：`output/config-admin-display-deployed.json`。回退只恢复该目录的旧代码文件，并移除本次新增来源种子；不要恢复旧的数据目录覆盖后续发布。

## 2026-09-22 beta.7 正式目录

正式通道已发布 `20260922T154232-r11.json`，由已验证的预览 r10 生成，28 条模型内容逐字段一致，仅新增 `catalogScope` 限定接管已有五个视频 API 主机。预览仍为 r10。山海保持下架，模型参数、售价、中转站计费未调整。

新版桌面运行时约每 10 秒刷新；文字、图片及范围外自定义 API 延续原账号名单，旧节点和默认角色按原 model 精确绑定。安装模式真实 Electron 冒烟覆盖远程改名、分组、停用、空目录及旧账号保留，通过 1321 项单测和构建检查；未调用付费生成。

服务器 schema 已同步并重启服务，公网正式/预览 JSON 及服务状态验证正常。操作前数据和 schema 备份为 `/root/flow-config-backups/beta7-20260922T154212Z/`，原 r3 仍在版本历史中，可在管理页切回。

## 2026-09-22 快捷开关实测

新增运维 CLI `catalog-control.mjs`（无需重启 CONFIG 服务），支持精确模型/分组的启用、停用和按回执恢复。操作说明见 `docs/config-channel-control.md`，不联动中转站数据库。

真实预览测试执行 r5 停用/r6 恢复、r7 停用/r8 恢复及 r9 MiniMax 停用/r10 恢复。全部测试目标已恢复原启用状态，正式 r3 保持不变。首次自动同步出现后台计时延迟，源码版现由主进程发送检查心跳，修复后停用/恢复自动同步实测 8.17/9.20 秒；主动刷新快捷路径实测 4.05/3.42 秒。测试未调用任何付费生成接口。

## 2026-09-22 停用卡片保留

管理平台默认保留 `catalog.enabled:false` 的模型卡片，暗红色标注“已停用”；组内全部停用时保留组卡片并显示“分组停用”，部分停用时显示数量。卡片仍可点击编辑和重新启用。`presentation.visible:false` 继续由“显示隐藏项”控制，画布候选过滤和任务启用规则不变。

仅更新远端 `lib/admin-catalog-model.mjs` 与 `lib/admin-catalog-view.mjs`，备份及核对记录位于 `/root/flow-config-backups/disabled-cards-20260922T131239Z/`。正式 r3、源码预览 r4、全部版本记录及管理密码摘要保持不变。定向单测和隔离浏览器的停用/发布/刷新/恢复流程通过，桌面与移动端布局已核对。

## 2026-09-22 操作平台与源码预览

在 `154.12.57.162:22` 更新了 `/srv/flow-config` 的管理模块、schema 和 SVG 图标。远端管理页新增与画布对应的分组操作平台，以及“正式配置 / 源码预览”两个发布通道。

- 正式配置：`https://artconfig.ravenhash.org/config`，仍为 `20260922T103908-r3.json`，18 条配置，文件内容逐字不变。
- 源码预览：`https://artconfig.ravenhash.org/config/preview`，已发布 `20260922T124222-r4.json`，28 条配置、23 条精确目录映射、16 条启用映射。
- 预览管理：`https://artconfig.ravenhash.org/admin?channel=preview`。未登录、密码错误重试及登录成功都保留预览通道；网页登录继续使用原管理密码。

预览以实际 r3 为基础，保留其 18 条模型的参数、能力和价格；补齐展示与精确目录映射、已有 StarFrame 备用渠道及 2.0 Fast/Mini/Pro。Yueqi 三个新模型采用线上已经生效的模型 ID 和配置。山海及此前停用渠道保持禁用。预览发布、清空和回滚均不改变正式指针；两条通道的生效版本都受删除保护。

源码试验版默认地址已切换到远端预览，使用独立资料 `%APPDATA%/flow-canvas-config-lab`、MCP 端口 `18766`，本地账号模型名单仍为零，每 10 秒读取远程目录。原安装版继续读取正式配置，本次未发布桌面安装包。

备份：`/root/flow-config-backups/catalog-ui-20260922T123133Z/`，包含代码、配置数据、服务单元、原文件摘要、部署文件摘要和预览输入。部署只覆盖管理代码、schema、图标；Node、依赖、服务单元、环境文件、种子与管理密码均保留。两次代码切换后均校验现行文件和密码摘要不变。

验证：服务器语法检查及完整 schema 校验通过；公网健康接口、正式与预览 JSON 均返回正常；源码实例实际读取预览 r4，包含新增 Yueqi 目录映射。隔离测试覆盖通道发布、清空、回滚、登录跳转和桌面操作流程，未调用付费生成。回退代码时使用代码备份；不要覆盖上线后的 state、版本文件或账号设置。

## 2026-09-13

- 管理入口：`https://artconfig.ravenhash.org/admin`。
- 服务器：`154.12.57.162`，SSH 用户 `root`，端口 `22`。凭据不写入项目。
- 系统服务：`flow-config.service`，仅监听 `127.0.0.1:8087`；Caddy 提供 HTTPS 反代。
- 代码：`/srv/flow-config`；数据和管理密码哈希：`/var/lib/flow-config`。
- 运行时：`/srv/flow-config/runtime/node`，本次复制服务器已有的 Node `v24.21.0`。
- 升级前备份：`/root/flow-config-backups/20260913-ui-deploy/before.tgz`，包含旧代码、数据、服务单元、环境配置和 Caddy 配置。
- 本次上线模型编辑表单和 UI 遮挡修复，未更改中转渠道、计费或管理员密码。

502 的直接原因是 systemd 找不到 Node。此前 Node 仅安装在 root 的 NVM 目录，
服务持续以 `status=127` 退出。安装器现使用独立运行时路径，保留已有环境配置，并在升级后明确重启。
服务关闭 `MemoryDenyWriteExecute`，以兼容 V8 JIT；其余已有 systemd 隔离设置保留。

部署前数据目录只有 `admin.json`，没有现行版本。此次保留服务器原始种子模型表并在成功启动后
生成 `20260913T153158-r0.json`（16 个模型），没有用本地开发中的模型表覆盖它。
以后更新服务代码，不等于发布新的模型配置；模型变更仍通过管理面板单独发布。

验证：公网 `/health` 与 `/config` 返回 200，ETag 条件请求返回 304，登录页浏览器加载正常；
线上编辑器文件 SHA256 与本地修复一致。重启后现行版本和 `state.json` 未变，服务已设置开机启动。
原始模型表、管理密码文件、环境配置和 Caddy 配置的 SHA256 与部署前一致。
没有使用现有管理员密码进行线上登录或发布测试；完整表单流程此前在隔离环境验证通过。

需要回退代码时先备份最新数据，只恢复所需代码与服务文件，不要用旧备份覆盖上线后的配置版本。
旧服务单元包含导致 502 的运行路径问题，不应原样恢复。

## 2026-09-14 模型名单校正

前次部署保留的旧种子表仍含未上架模板，并遗漏已上架的三个 HM 模型。经用户要求，仅针对名单
通过 `/admin` 发布 `20260913T155903-r1.json`（15 条配置）：移除 Seedance 1.5、Wan、Kling、
Tencent/Vidu 的四个 `video-template.*` 条目，补齐 HM 933、101010、301010。
保留的 12 条模型配置逐字段不变；新增 HM 的人民币售价分别为 6.5、7、10 元/次。

已验证公网 CONFIG、登录后的表单名单、HM 币种/售价和发布栏布局；未修改中转渠道或实际计费。
服务器 `seed/model-config.default.json` 同步为此次已发布配置，避免空数据初始化时再次引入旧模板。
旧版本仍留在版本记录供回滚，旧种子另备份于
`/root/flow-config-backups/20260913-ui-deploy/seed-before-catalog-r1.json`。
此次发布不依赖客户端重新打包；客户端可手动刷新 CONFIG，或等待既有自动刷新。
