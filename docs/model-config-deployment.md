# CONFIG 生产部署记录

## 2026-09-13

- 管理入口：`https://artconfig.ravenhash.org/admin`。
- 服务器：`154.12.57.162`，SSH 用户 `root`，端口 `22`。凭据不写入项目。
- 系统服务：`flow-config.service`，仅监听 `127.0.0.1:8087`；Caddy 提供 HTTPS 反代。
- 代码：`/srv/flow-config`；数据和管理密码哈希：`/var/lib/flow-config`。
- 运行时：`/srv/flow-config/runtime/node`，本次复制服务器已有的 Node `v24.21.0`。
- 升级前备份：`/root/flow-config-backups/20260913-ui-deploy/before.tgz`，包含旧代码、数据、服务单元、环境配置和 Caddy 配置。
- 本次上线模型编辑表单和 UI 遮挡修复，未更改中转渠道、计费或管理员密码。

502 的直接原因是 systemd 找不到 Node。此前 Node 仅安装在 root 的 NVM 目录，
服务持续以 `status=127` 退出。安装器现使用独立运行时路径，保留已有环境配置，并在升级后明确重启。
服务关闭 `MemoryDenyWriteExecute`，以兼容 V8 JIT；其余已有 systemd 隔离设置保留。

部署前数据目录只有 `admin.json`，没有现行版本。此次保留服务器原始种子模型表并在成功启动后
生成 `20260913T153158-r0.json`（16 个模型），没有用本地开发中的模型表覆盖它。
以后更新服务代码，不等于发布新的模型配置；模型变更仍通过管理面板单独发布。

验证：公网 `/health` 与 `/config` 返回 200，ETag 条件请求返回 304，登录页浏览器加载正常；
线上编辑器文件 SHA256 与本地修复一致。重启后现行版本和 `state.json` 未变，服务已设置开机启动。
原始模型表、管理密码文件、环境配置和 Caddy 配置的 SHA256 与部署前一致。
没有使用现有管理员密码进行线上登录或发布测试；完整表单流程此前在隔离环境验证通过。

需要回退代码时先备份最新数据，只恢复所需代码与服务文件，不要用旧备份覆盖上线后的配置版本。
旧服务单元包含导致 502 的运行路径问题，不应原样恢复。

## 2026-09-14 模型名单校正

前次部署保留的旧种子表仍含未上架模板，并遗漏已上架的三个 HM 模型。经用户要求，仅针对名单
通过 `/admin` 发布 `20260913T155903-r1.json`（15 条配置）：移除 Seedance 1.5、Wan、Kling、
Tencent/Vidu 的四个 `video-template.*` 条目，补齐 HM 933、101010、301010。
保留的 12 条模型配置逐字段不变；新增 HM 的人民币售价分别为 6.5、7、10 元/次。

已验证公网 CONFIG、登录后的表单名单、HM 币种/售价和发布栏布局；未修改中转渠道或实际计费。
服务器 `seed/model-config.default.json` 同步为此次已发布配置，避免空数据初始化时再次引入旧模板。
旧版本仍留在版本记录供回滚，旧种子另备份于
`/root/flow-config-backups/20260913-ui-deploy/seed-before-catalog-r1.json`。
此次发布不依赖客户端重新打包；客户端可手动刷新 CONFIG，或等待既有自动刷新。
