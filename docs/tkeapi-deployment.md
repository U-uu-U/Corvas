# 视频中转站部署记录

## 2026-09-21 新站代理分成调价

用户确认代理按成交金额抽成，从 20% 调整到 30%，仅修改新站 `cart.ravenhash.org`。除 `seedance-2.5-pro` 外，已接入的自定义模型计费单价按原价乘以 `0.8 / 0.7`，统一向上保留两位小数。在上游成本和原有折扣不变时，用户每笔成交扣除代理分成后的收入不低于原来；向上取整会有少量增加。

- `seedance_v2.5`：5.00 → 5.72 元/次。
- `sd2.5`、`sd2.5-route1`：6.00 → 6.86 元/次。
- `seedance_v2.0-933`：6.50 → 7.43 元/次。
- 已停用的 101010 / 301010：7.00 → 8.00、10.00 → 11.43 元/次，保持停用。
- `seedance-2.5-pro`：480p / 720p 均为 1.25 元/秒，30 秒预扣 37.50 元。
- MiniMax H3：480p 为 0.06，768p（含 786p 兼容别名）为 0.14，1080p / 2k 为 0.24，4k 为 0.29 元/秒；保留旧超分档位别名。
- 海外 2.0 的有/无视频输入计费单价也按同一公式更新，保留 token 计费方式和原有 0.9 折扣。Fast 720p：25.46 / 43.20；Mini 720p：16.20 / 27.00；Pro 720p：33.18 / 54.00、1080p：36.26 / 59.40、4k：18.52 / 30.86，单位沿用原规则。

运行 `server/seedance-hm/reprice-cart.py --apply`，修改 10 条计费规则、11 个模型的预扣金额及相关价格说明。三个停用的系统示例模型不改动。调整的是模型售价；未修改代理账户、分佣插件、用户余额、历史账单、充值或付款记录。老站只读核对，没有写入。

事务预演与正式提交后逐字段比较通过，原有渠道、凭据、转发、分组、站点设置及插件配置摘要不变。正式备份和定向回退 SQL：`/root/tkeapi-backups/commission-pricing-20260921T025208Z/`。脚本含重复执行保护及并发配置检查。

Corvas 画布模型列表与 Agent 计划卡不再显示单价、估算价格或未知费用占位。新站已知内部价格同步供 MCP/Agent 使用，老站内部价格保持原值；本次未重新发布安装包。

## 2026-09-21 新站模型目录同步

在 `cart.ravenhash.org`（`154.12.57.129:26098`，部署目录 `/root/tkeapi`）同步最新视频模型目录。新增 `seedance-2.5-pro`，复用新站已有主播渠道及转发规则，人民币按秒计费：480p 和 720p 均为 1.06 元/秒，30 秒预扣 31.8 元。现有模型价格、折扣及预扣金额不改动。

前台服务商、后台 API 供应商、渠道分类统一为 `MiniMax H3`、`Seedance 2.5 推荐渠道`、`Seedance 2.5 备用渠道`、`Seedance 2.0 推荐渠道`，按此顺序显示。推荐组前两项为 `sd2.5`（电商效果优化）和 Pro（满血满参）；备用路线标注“过人脸”；海外 2.0 模型补全 Fast、Mini、Pro，分组说明为“可NSFW 无限制”。MiniMax 保留原有独立分类。

`seedance_v2.5-101010`、`seedance_v2.5-301010` 同步为停用。三个原有但未绑定任何渠道的系统示例模型 `doubao-seedance-2-0-260128`、`doubao-seedance-2-0-fast`、`doubao-seedance-2-5-260628` 也按老站状态停用。同步后共 14 个模型记录，其中 9 个启用、5 个停用；模型和历史记录均未删除。

部署脚本为 `server/seedance-hm/sync-cart-models.py` 与 `sync-cart-models.sql`。事务预演回滚、正式提交及逐字段核对均通过。既有计费规则、渠道凭据、接口、转发规则保持不变；用户、令牌、站点设置（含存储配置）、插件等业务配置摘要不变。没有重建或重启服务，五个容器的 ID / 镜像 ID 不变且健康检查正常，首页和 `/api/health` 返回 200。

模型广场接口要求登录（401），没有适合只读核对的现成 API 令牌，因此未创建令牌，也未声称完成登录页面或付费生成验证。启用数量、分组顺序、渠道绑定和计价通过数据库回读确认。老站本次仅作查询。

正式备份：`/root/tkeapi-backups/catalog-sync-20260920T165844Z/`（UTC 命名），包含整库备份、前后配置、迁移 SQL、核对结果和定向 `restore.sql`。目录仅 root 可读。回退只恢复本次调整字段并停用新增模型和分组，不覆盖整库；保留新增计费规则和模型记录以供历史追溯。

## 2026-09-17 新站供应商分组

仅调整 `cart.ravenhash.org` 的分组展示，不改老站。统一为 `SD2.5 线路一`、`其他 SD 视频`、`MiniMax H3` 三组：`sd2.5-route1` 单独归线路一，`minimax-h3` 归 H3，其余 SD 模型归其他 SD 视频。当前启用模型数分别为 1 / 11 / 1；其中包含新站原有的 3 个默认 Doubao 模型，本次只调整它们的分类，没有增加渠道绑定或修改启用状态。实际渠道数分别为 1 / 2 / 1，主播与海外两条渠道仍保持独立接口和凭据。

现有版本前台模型广场使用 `models.provider_id`，后台 API 供应商使用 `models.api_provider_id`，渠道及上游预设使用 `category_id`。为三处建立同名分组，复用三条 API 供应商和三条渠道分类记录，新增三条前台展示服务商记录。系统内置官方厂商记录原样保留，未删除。H3 原 API 供应商名中的上游 IP 已替换为 `MiniMax H3`。

事务预演及正式提交后逐字段对比通过：仅修改分组、名称、展示排序和对应关联，模型 ID、渠道模型列表、接口、密钥、预设绑定、计价、折扣、转发规则保持不变。`settings`（含存储桶）、账户、余额、令牌、插件配置摘要不变；前后端、数据库和两适配器的容器 ID / 镜像 ID 不变。健康接口正常，没有付费生成。前台模型广场接口要求登录，本次未用客户身份做页面验收；该接口正常缓存周期为 60 秒。

备份：`/root/tkeapi-backups/supplier-groups-20260917T090904Z/`，含修改前整库备份、分组快照、导入与恢复 SQL、核对结果。恢复脚本只回退本次分组字段并停用新增展示服务商，不覆盖之后的业务数据。

## 2026-09-17 新站渠道同步

将 `art.ravenhash.org` 的 4 条启用渠道及 2 份渠道预设同步至 `cart.ravenhash.org`。新站原先没有渠道；新增关联的 10 个模型、3 个 API 厂商条目、3 条转发规则和 9 条计费规则。按稳定模型标识关联，数据库主键重新映射，不照搬老站编号。新站原有 3 个模型及其规则不改动，老站不写入。

新站以 CNY 记账，老站以 USD 记账且 CNY 汇率为 6.75。因此仅对新增规则的金额、单价和模型预扣金额乘以 6.75，保持实际人民币售价一致，不修改新站全局币种或现有价格。固定计费为 `seedance_v2.5` 5 元、`sd2.5-route1` / `sd2.5` 6 元、HM 933 / 101010 / 301010 分别 6.5 / 7 / 10 元。保留原折扣倍率，渠道消耗统计从零开始，不迁移历史任务或消耗。

新站部署目录 `/root/tkeapi`。前端、后端和 PostgreSQL 的容器 ID、镜像 ID、挂载保持不变，没有更新版本或重建这些容器。`settings`（包含 `storage_settings`）、用户、API 令牌、订单、充值、用户等级、插件及插件配置逐表摘要比较不变；没有修改或访问存储桶内容。

新增两个独立的本机适配服务，配置为 `/root/tkeapi/channel-adapters/compose.yml`，仅加入既有 `tkeapi-network`，不发布主机端口。H3 和 968 适配源码直接复制老站，SHA-256 相同；Python 运行时镜像从老站复制，固定为 `sha256:d09d15e60962ca365d1cd544a48773bac9d33f2fb1b00f2aa0deec78ade7dc31`，禁止自动拉取更新。

968 结果改由 `https://cart.ravenhash.org/fc-media/files/<hash>.mp4` 回传。Caddy 仅新增匹配该路径的静态文件路由，既有反代配置不变。文件存于独立临时目录 `/var/lib/corvas-channel-media`，不是对象存储桶；`cart-channel-media-cleanup.timer` 每小时清理超过 24 小时的结果及残留分片。

验证：数据库事务预演回滚成功，正式导入逐字段比较成功；两适配器健康，H3 自检、968 固定 30 秒参数转换及素材顺序自检通过。公网健康与管理页返回 200，独立测试视频下载返回 200，Range 下载返回 206，内容哈希一致，测试文件已移除。没有提交真实付费生成，也没有新建客户 API 令牌。

新站备份目录：`/root/tkeapi-backups/art-channel-sync-20260917T074414Z/`，仅 root 可读，含导入前数据库备份、原部署文件、渠道配置快照、导入 SQL、编号映射与 `verification.json`。其中含私密配置，不下载到公开目录或提交仓库。需要停用时优先停用这次导入的渠道，不用整库备份覆盖之后的客户数据。

## 2026-09-14 升级

- 站点：`https://art.ravenhash.org`；管理入口：`https://art.ravenhash.org/admin1688`。
- 部署：SSH 配置 `hk-direct`，`/opt/tokensbyte`。本次不涉及 `ai.ravenhash.org` 和独立 CONFIG 服务。
- 原前后端镜像分别构建于 2026-07-31 和 2026-07-30；升级到 TkeApi 发布版 `v20260908`。
- 安装来源：官方仓库 tag 对应提交 `ea58706fc3c000f554d36f2549d7d4ba8b7905df` 的 `dockerimage/tkeapi-offline-20260908_104821.tar.gz`。
- 安装文件 SHA256：`de0fa24de4e6a60db2ac4b6d891659cdce180538a479b1154f83384d289fb8d6`；Git blob：`0e1c442f3a4c0de2ce8eddc558c0f2de1b4ec1f0`。
- 新后端镜像：`corvas-relay/backend:v20260908`，ID `sha256:6428636a63a8810cfd48b3d373c91464d0677060d553e515e8dc9e8175b5d45f`。
- 新前端镜像：`corvas-relay/frontend:v20260908`，ID `sha256:2f2ed07d2d6acebd4e150d6f7a80da35684de343c67d05a71c48217dc636dda6`。
- 官方镜像内的构建提交为 `474c324553a5214ebb142215f3b2cf86553f8b6e`，构建日期 2026-09-08。后台关于页显示的 `v1.0.10` 是它自己的提交序号，并非发布 tag；判断版本应同时看日期、提交和镜像 ID。

## 保留的配置

保持原 `docker-compose.yml`、`.env`、数据库版本、网络、挂载与密钥不变，通过自动加载的 `docker-compose.override.yml` 固定新版前后端镜像，禁止自动拉取浮动版本。增加 `CORS_ORIGINS=https://art.ravenhash.org`。

已有 10 个启用模型、4 条渠道及关联的转发规则保留。新版迁移自动新增的三个 Doubao 模型保持停用，没有向客户上架新模型；原模型的 `feature_attributes` 保留，避免未经确认地改成首尾帧能力。

SD 双线路与 HM 的人民币售价不变：固定 30 秒双线路 6 元/次，`seedance_v2.5` 5 元/次，HM 933 / 101010 / 301010 分别为 6.5 / 7 / 10 元/次。原有账户、余额、API 令牌、渠道密钥、模型映射及汇率配置逐字段比较通过。

H3 计费规则 17 原有档位名称为 `786p`、`2k超分`、`4k超分`，与客户端发送的 `768p`、`2k`、`4k` 不匹配。新增这三个兼容别名，复制对应旧档位的单价、启用状态及其他属性；旧名称也保留，不修改历史任务含义或费用。

`seedance968-adapter`、`minimax-h3-adapter`、上传服务和 MinIO 未重建。服务器源码目录里的旧 `backend/src/relay/forward.rs` 本地修改已备份并保留；实际服务使用上述固定镜像，不要从这份旧源码执行 `--build` 覆盖新版。

## 核对结果

先在无外网的独立 Docker 网络中恢复数据库备份，运行新版迁移。迁移记录由 72 条增加到 204 条。通过模拟上游验证 SD 双线路、独立可调时长线路、三个 HM 规格，以及 H3 的 768p / 2k / 4k 提交和失败查询；错误原因保留，失败后模拟余额不变。没有向真实上游提交付费生成。

切换前确认没有未完成任务；停止入口与后端后再次备份，再升级和校正配置。切换窗口：2026-09-14 08:05:35 至 08:05:47 UTC，约 12 秒。

上线后验证公网健康接口、管理页面及 JS/CSS 资源均返回 200；鉴权模型列表仍为原 10 个模型。用已有成功和失败任务分别查询，原任务 ID 保留，成功返回 completed，失败带明确原因，账户余额未因查询改变。前后端与 PostgreSQL 健康，两个适配器容器 ID 未改变。

临时测试容器、独立网络及数据库副本已清理。此次没有重新发布 Corvas 安装包，也不代表客户旧安装包已包含本地尚未发布的错误提示修复。

## 运维与回退

备份目录：`/opt/tokensbyte-backups/upgrade-v20260908-20260914T075124Z/`，仅管理员可读。保留原始 `before.dump`、正式切换前 `cutover.dump`、部署文件与数据目录备份、旧二进制、镜像信息、配置快照、迁移比较和上线核对报告；目录内含私密配置，不上传仓库。

正常从 `/opt/tokensbyte` 运行 Docker Compose，会自动读取 override。显式指定文件时必须同时指定 `docker-compose.yml` 与 `docker-compose.override.yml`，否则可能重新启用旧镜像。

升级已执行数据库迁移。回退前先备份最新数据并停止业务写入，检查旧程序与现有数据库的兼容性；不能只换回旧镜像就认定回退完成，也不能拿升级前整库直接覆盖上线后的余额与任务。旧镜像以 `corvas-rollback/tokensbyte-backend:20260914T075124Z` 与 `corvas-rollback/tokensbyte-frontend:20260914T075124Z` 保留。
