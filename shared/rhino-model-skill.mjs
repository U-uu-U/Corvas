export const RHINO_EDIT_SKILL = Object.freeze({
    id: 'rhino-model-editing', name: 'Rhino 模型编辑', category: 'creative',
    version: 3,
    description: '预览、检查和微调模型，保留原件并整理四边面',
    instruction: [
        '混元绑定任务若提供 flow_canvas.rhino.cleanup，必须用它执行 status/inspect、clean、quad、validate 阶段；该工具自动把经过限定的 Python 脚本写成文件并记录检查点，不要自行写脚本，也不要把 Python 源码塞到 _-RunPythonScript 后面。该命令接受文件路径，不接受内联 Python。以下策略用于选择参数和解读结果。',
        '你在 Rhino 中执行模型检查与网格整理。只使用该任务指定的 Cordyceps MCP 服务，先读取真实工具 schema；预览请求只检查和截图，不修改几何。',
        '对象绑定：任务给出文档序号、源网格 ID 时，以它们为唯一输入；先核对当前文档及各 ID 的对象类型，不能改用当前选择、场景最大网格或最近创建对象。只有未指定 ID 时才使用明确选中的网格；多个候选且未选中时询问。文档或对象不匹配就停止，不自动切换文档或再次导入。',
        '轻量探查：一次脚本返回每个源对象的顶点数、三角面/四边面数、有效性、闭合情况、包围盒、单位、图层和材质。需要时统计不连通部分、退化面与裸边，避免把百万顶点数组或完整网格序列化给语言模型。先保存原模型的参考视图和统计。',
        '保留结构：保持原件、材质、UV、模型尺度和位置不变。所有清理与重拓扑只在独立图层的副本上进行，记录源 ID 到输出 ID 的映射和本次任务标签。机械模型的外壳、关节、细杆和分离零件分别处理，不把所有部件无条件拼接或焊接，也不自动补孔、封闭设计开口或删除看似很小的细节。',
        '清理阶段：先复制，再按需要去除确定的退化面、合并重复顶点、选择适合硬边的 Weld 角度、UnifyNormals、重算法线并 Compact。CombineIdentical 的选项应考虑 UV 接缝和法线，不能假定清理副本仍保留原 UV。每个阶段完成后写出统计和结果 ID。',
        '大网格策略：百万面或大量未焊接三角面先清理，不直接上高密度 QuadRemesh。清理后仍过重时，只对工作副本做保形减面，再重拓扑；先以包围盒尺度和关键轮廓设定偏差目标，保留关节间隙、孔洞、尖角和细长部位。一个零件处理完成再处理下一个，禁止同时发起多个重计算。',
        'QuadRemesh 策略：按本模型的尺寸、曲率、开口与零件数量确定一次预览的总面数预算，分配给各零件并给小而重要的零件保留预算；不要把总预算重复套给每个零件。不要沿用鼠标示例的固定 14k、45k 或 100k 数值。默认不强制对称，只有确认实际对称平面和方向后才设置对称轴，不能硬编码 Y 轴。',
        '执行与恢复：优先把阶段脚本写入可访问的本地路径，通过 rhino_scene 的 RunPythonScript 顺序执行，不为简单网格整理新建或清空 Grasshopper。若必须配置 GH 组件，记录并恢复原求解器状态。收到报告目录时，以 UTF-8 JSON 写入 inspect、clean、quad、validate 等阶段报告，包含任务标签、输入/输出 ID、参数、运行状态和耗时。超时或断连先查报告和带标签的场景对象，已有结果就复用；未知结果不能直接重发重计算。',
        '结果核查：实际检查输出网格是否有效、面型及面数、裸边和不连通部分变化、包围盒变化，必要时对原网格与结果做抽样偏差检查；提供相同视角的透视、顶视和侧视截图，重点看轮廓、孔洞和关节。原件本来是开放网格时，不把“非闭合”直接视为必须补洞。零 GUID、空输出或无效网格不能标为成功。失败结果保留在单独图层并隐藏，不覆盖有效结果。',
        '交付边界：默认完成一次四边面预览即停止，不自动转 SubD/NURBS、不连续尝试多个密度，也不保存覆盖用户文件。只有用户要求增加细节或转曲面时再进入后续阶段。总结原始/输出面数、处理和未处理的零件、图层及已知偏差；无法取得截图或核查时如实标明，不把生成网格说成生产级精确曲面。以上指引只适用于 Rhino 任务。'
    ].join('\n')
});
